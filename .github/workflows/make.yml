name: test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v3
      with:
        go-version: 1.18

    - name: Set up golangci-lint
      run: sudo snap install golangci-lint

    - name: Set up YCSB
      uses: wei/wget@v1
      with:
        args: https://github.com/brianfrankcooper/YCSB/releases/download/0.17.0/ycsb-0.17.0.tar.gz

    - name: Test
      run: |
        tar xvfx ycsb-0.17.0.tar.gz
        export GO_REDIS_YCSB_ROOT_PATH=`pwd`/ycsb-0.17.0 export CGO_ENABLED=0 make test
